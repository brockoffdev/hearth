/**
 * SetupFamilyPlaceholder — temporary route for /setup/family.
 *
 * TODO Phase 2 Task F: Replace this entire file with the real family-calendar
 * mapping step (SetupFamily). This placeholder is scaffolding for the wizard
 * routing structure.
 *
 * Inline styles are intentional here: this component is throwaway scaffolding
 * and CSS modules would be misleading overhead.
 */
export function SetupFamilyPlaceholder() {
  return (
    // eslint-disable-next-line react/forbid-dom-props
    <div style={{ padding: 40 }}>
      <h1>Wizard Step 3 — Coming in Phase 2 Task F</h1>
      <p>Family calendar mapping will live here.</p>
    </div>
  );
}
